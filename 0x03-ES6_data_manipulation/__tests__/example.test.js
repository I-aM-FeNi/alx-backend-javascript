// __tests__/example.test.js

describe('Basic Tests', () => {
    test('Simple test to check true is true', () => {
        expect(true).toBe(true);
    });

    test('Sample addition test', () => {
        expect(1 + 1).toBe(2);
    });
});
